[package]
name = "stinger-rwlock-watch"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.41", features = ["sync", "rt", "macros", "time", "rt-multi-thread"] }

[dev-dependencies]
tokio = { version = "1.41", features = ["full"] }
